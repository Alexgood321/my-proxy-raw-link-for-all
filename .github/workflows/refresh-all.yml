name: Shadowrocket Refresh All

on:
  schedule:
    - cron: "*/5 * * * *"   # каждые 5 минут
  workflow_dispatch:        # ручной запуск из Actions

permissions:
  contents: write

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Update timestamp in all module/list files
        shell: bash
        run: |
          set -euo pipefail

          ts="$(date -u +"%Y-%m-%d_%H:%M:%S_UTC")"
          changed=0

          # Список кандидатов: типичные текстовые конфиги + всё в rules/ + файл gg без расширения
          mapfile -t files < <(git ls-files \
            | grep -E '(\.(conf|list|module|yaml|yml|txt|ini|cnf)$)|(^rules/)|(^gg$)' \
            | grep -Ev '^\.github/')

          for f in "${files[@]}"; do
            # Пропускаем бинарные и большие файлы (>2MB), на всякий
            if [ ! -s "$f" ] || [ $(stat -c%s "$f") -gt 2000000 ]; then
              continue
            fi
            if ! file --mime "$f" | grep -q 'text/'; then
              continue
            fi

            # Комментарий в начале файла безопасен для YAML, list, module и др.
            if grep -qE '^(#\s*)?last_update=' "$f"; then
              sed -i '1s/^\(#\s*\)\?last_update=.*/# last_update='"$ts"'/' "$f"
            else
              printf '# last_update=%s\n%s' "$ts" "$(cat "$f")" > "$f"
            fi
          done

          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add -A

          if ! git diff --cached --quiet; then
            git commit -m "Auto refresh (cache-bust) at $ts"
            git push
            echo "Changes committed."
          else
            echo "No changes to commit."
          fi
